import type { Metadata } from "next";
export const metadata: Metadata = { title: "Simulator Cicilan — Cicilan Saya Wajar?", description: "Hitung cicilan bulanan, rasio utang-pendapatan, dan kesehatan keuangan kamu." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
