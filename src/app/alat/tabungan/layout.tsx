import type { Metadata } from "next";
export const metadata: Metadata = { title: "Kalkulator Tabungan — Berapa Lama Nabung?", description: "Rencanakan tabungan kamu dan lihat berapa lama mencapai goal." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
