import type { Metadata } from "next";
export const metadata: Metadata = { title: "Perbandingan Biaya Hidup — Kota X vs Y", description: "Bandingkan biaya hidup di 10 kota besar Indonesia: makanan, transportasi, sewa, utilitas." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
