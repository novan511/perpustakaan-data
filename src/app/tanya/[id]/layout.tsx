import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tanya Data — Detail Jawaban", description: "Jawaban pertanyaan tentang Indonesia berbasis data dan fakta." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
